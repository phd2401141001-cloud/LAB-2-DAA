name: Autograding

on: [push]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Make script executable
        run: chmod +x check.sh
        
      - name: Run Autograder
        run: ./check.sh
