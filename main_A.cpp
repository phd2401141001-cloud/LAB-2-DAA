#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

// TODO: Implement Logic for Problem A (Numerical Verification)

void solve(int id) {
    // Your code here
    // Output exactly 3 lines: Big-O: ..., Big-Omega: ..., Theta: ...
}

int main() {
    int T;
    if (cin >> T) {
        while(T--) {
            int id;
            cin >> id;
            solve(id);
        }
    }
    return 0;
}
